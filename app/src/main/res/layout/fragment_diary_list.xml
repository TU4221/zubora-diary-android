<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />
        <variable
            name="viewModel"
            type="com.websarva.wings.android.zuboradiary.ui.diary.list.DiaryListViewModel" />
    </data>

    <FrameLayout
        style="@style/AppRootViewStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <com.google.android.material.appbar.AppBarLayout
                style="@style/AppAppBarLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" >

                <com.google.android.material.appbar.MaterialToolbar
                    style="@style/AppToolbarStyle"
                    android:id="@+id/material_toolbar_top_app_bar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:menu="@menu/diary_list_toolbar_menu"
                    app:navigationIcon="@drawable/ic_calendar_month_24px"
                    app:navigationContentDescription="@string/fragment_diary_list_toolbar_navigation_top_year_month_switching"
                    app:onNavigationIconClick="@{() -> viewModel.onNavigationIconClick()}"
                    app:title="@string/fragment_diary_list_toolbar_title"
                    app:applySystemInsetsToPaddingLeft="@{true}"
                    app:applySystemInsetsToPaddingRight="@{true}" />

            </com.google.android.material.appbar.AppBarLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <TextView
                    style="@style/AppTextViewStyle"
                    android:id="@+id/text_no_diary_message"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:textAppearance="@style/AppTextViewTextAppearance"
                    android:text="@string/fragment_diary_list_no_saved_diary_message"
                    android:gravity="center"
                    android:visibility="@{viewModel.uiState.hasNoDiaries ? View.VISIBLE : View.INVISIBLE}"
                    tools:visibility="visible"/>

                <!-- MEMO:スクロールバーを表示させたいが、子RecyclerViewに反応してスクロールバーの長さが安定しない。
                          その為、"android:scrollbars="none"" とする。 -->
                <com.websarva.wings.android.zuboradiary.ui.common.view.SwipeBackgroundButtonRecyclerView
                    style="@style/AppRecyclerViewStyle"
                    android:id="@+id/recycler_diary_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="@{viewModel.uiState.hasNoDiaries ? View.INVISIBLE : View.VISIBLE}"
                    tools:visibility="visible"/>

            </FrameLayout>

        </LinearLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            style="@style/AppMainFloatingActionButtonStyle"
            android:id="@+id/floating_action_button_diary_edit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|bottom"
            android:layout_marginEnd="@dimen/fab_end_spacing"
            android:layout_marginBottom="@dimen/fab_bottom_spacing"
            android:src="@drawable/ic_edit_24px"
            android:contentDescription="@string/fragment_diary_list_fab_new_diary_creation"
            android:enabled="@{!viewModel.uiState.isRefreshing}"
            android:onClick="@{() -> viewModel.onDiaryEditButtonClick()}"
            app:applySystemInsetsToMarginRight="@{true}" />

    </FrameLayout>


</layout>
