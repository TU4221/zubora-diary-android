<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 通知権限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- 位置情報利用権限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ネットワーク利用権限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ZuboraDiaryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeColorWhite"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="36"
        tools:ignore="RemoveWorkManagerInitializer">
        <!-- MEMO:HiltでWorkManagerをカスタム初期化するため、Lintの'RemoveWorkManagerInitializer'警告は意図的に無視。 -->

        <!--
        hiltを使用してWorkerManagerを使用する場合下記設定が必要
        https://developer.android.com/develop/background-work/background-tasks/persistent/configuration/custom-configuration?authuser=1&hl=ja
         -->
        <!--
        MEMO:androidx.startup.InitializationProviderを全て除くと
             ApplicationのLifeCycleObserverが機能しなくなった為、個別設定を行う。
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove">
        </provider>
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--MEMO:"android:windowSoftInputMode="adjustNothing"は、
                 "BottomNavigationView"をキーボードの上に表示させないのと,
                 フォーカスしたEditTextがキーボード表示時に隠れないようにActivityLayoutが上へ動く(Fragment内のToolbarが隠れる)のを防ぐ為の設定-->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
